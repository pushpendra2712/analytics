mtcars # control + enter to execute a code
?mtcars# data fram data structure
?mean
class(mtcars) #control + l to clear console

summary(mtcars)#summary of data set

str(mtcars)#structure of data set
table(mtcars$gear)

table(mtcars$carb)

table(mtcars$vs)

table(mtcars$cyl)#dollar is used to denote the column form which data is to be take

table(mtcars$cyl,mtcars$gear)#for cross query use comma to choose what all field to take


